% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/momocs_cl-helpers.R
\name{which_out}
\alias{which_out}
\title{Identify outliers}
\usage{
which_out(x, conf, nax, ...)
}
\arguments{
\item{x}{object, either Coe or a numeric on which to search for outliers}

\item{conf}{confidence for dnorm (1e-3 by default)}

\item{nax}{number of axes to retain (only for Coe),
if <1 retain enough axes to retain this proportion of the variance}

\item{...}{additional parameters to be passed to PCA (only for Coe)}
}
\value{
a vector of indices
}
\description{
A simple wrapper around \link{dnorm} that helps identify outliers. In particular,
it may be useful on \code{Coe} object (in this case a PCA is first calculated) and also
on \code{Ldk} for detecting possible outliers on freshly digitized/imported datasets.
}
\note{
experimental. dnorm parameters used are \code{median(x), sd(x)}
}
\examples{
# on a numeric
x <- rnorm(10)
x[4] <- 99
which_out(x)

# on a Coe
t <- seq(0, 2 * pi, length.out = 120)
mk <- function(sx, sy) cbind((1 + sx) * cos(t), (1 + sy) * sin(t))
out <- Out(list(mk(0.00, 0.00), mk(0.03, -0.02), mk(-0.02, 0.04), mk(0.40, 0.40)))
bf <- efourier(out, 6)
which_out(bf)

# inspect outliers on a PC score vector
x_p <- PCA(bf)
which_out(x_p$x[, 1], conf = 0.5)
}
